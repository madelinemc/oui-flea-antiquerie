<h1>all markets</h1>
<% @markets.each do |market| %>
    <div class="market_item">
        <p><%= link_to "#{market.name}", market_path(market) %></p>
        <p><%= market.location %></p>
    </div>
<% end %>